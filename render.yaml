services:
- type: worker
  name: rabah-coupons-bot
  runtime: python
  pythonVersion: "3.11.9"  # هذا هو المهم!
  buildCommand: |
    apt-get update && apt-get install -y gcc g++ libxml2-dev libxslt-dev
    pip install -r requirements.txt
  startCommand: python main.py
  envVars:
    - key: TELEGRAM_TOKEN
      sync: false
    - key: APP_KEY
      sync: false
    - key: APP_SECRET
      sync: false
    - key: TRACKING_ID
      sync: false
    - key: EXTERNAL_DATABASE_URL
      sync: false
    - key: GEMINI_API_KEY
      sync: false
    - key: TELEGRAM_API_ID
      sync: false
    - key: TELEGRAM_API_HASH
      sync: false
    - key: TELEGRAM_SESSION_STRING
      sync: false
    - key: CHANNEL_HISTORY_LIMIT
      sync: false